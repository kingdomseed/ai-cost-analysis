# Model Ratings Sweep (2026-02-22)

Goal: collect **public, source-linked** model rating/benchmark data and define how it maps into:
- `apps/public-calculator/data/models.YYYY-MM-DD.json` (`ratings[]`)

Rules:
- Prefer official docs (Microsoft Learn / provider pages).
- Keep every metric’s `scale` and do not normalize across incompatible scales.
- Capture “download” paths (CSV/API) when available.

This folder contains per-agent notes (one file per agent).

